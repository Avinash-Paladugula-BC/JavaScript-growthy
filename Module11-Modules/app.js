// named import
import {add} from "./mathUtils.js";

// default import
import multiply from "./mathUtils.js";

// calling the imported methdos with the values
console.log(add(1,2));
console.log(multiply(2,3));